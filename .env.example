# Frontend configuration

# Production: absolute API base URL (optional). When set, the app will call this
# URL directly. When omitted, the app uses a relative "/api" path which works
# with the dev proxy and when UI is served from the same domain as the backend/gateway.
# VITE_API_URL="https://api.example.com"

# Dev-only: multi-service proxy mapping for Vite dev server.
# Define targets as full URLs (origin + optional base path). These create routes:
#   - VITE_PROXY_DEFAULT -> maps "/api" to the target
#   - VITE_PROXY_AUTH    -> maps "/api/auth" to the target
#   - VITE_PROXY_BILLING -> maps "/api/billing" to the target
# The base path (e.g., "/api/v1") is preserved via rewrite.

# Examples (uncomment and adjust):
# VITE_PROXY_DEFAULT="http://localhost:8081/api/v1"
# VITE_PROXY_AUTH="http://localhost:8081/api/v1"
# VITE_PROXY_BILLING="http://localhost:8082/api/v1"
# VITE_PROXY_PAYMENTS="http://localhost:8083/api/v1"
# VITE_PROXY_PRODUCTS="http://localhost:8084/api/v1"
# VITE_PROXY_ORDERS="http://localhost:8085/api/v1"
# VITE_PROXY_NOTIFICATIONS="http://localhost:8086/api/v1"

# Optional: Supabase (unused with Spring Boot backends)
# VITE_SUPABASE_URL=""
# VITE_SUPABASE_PUBLISHABLE_KEY=""

# Demo/dev helpers
# Allow logging in with admin/admin without calling backend
# VITE_ALLOW_DUMMY_LOGIN=true
