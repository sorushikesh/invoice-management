# Frontend configuration for Spring Boot integration

# Absolute URL of your API for production builds (e.g., when hosting static files)
# If omitted, the app uses a relative "/api" path which works with the dev proxy
# and when the UI is served by the same domain as the backend.
# VITE_API_URL="https://api.example.com"

# Dev-only: where to proxy "/api" requests during `vite` dev server
# Spring Boot default is http://localhost:8080
VITE_API_PROXY_TARGET="http://localhost:8080"

# Optional: existing Supabase variables are unused if you use Spring Boot
# VITE_SUPABASE_URL=""
# VITE_SUPABASE_PUBLISHABLE_KEY=""
